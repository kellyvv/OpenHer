# OpenHer Engine — Minimal dependencies for Docker microservice
# Excludes: TTS, skills, EverMemOS, auth, websockets, cron, testing

# Web framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
uvloop>=0.19.0
sse-starlette>=1.6.0
python-multipart>=0.0.9

# LLM providers
openai>=1.12.0
anthropic>=0.18.0
dashscope>=1.17.0
google-genai>=1.0.0

# Data & config
pydantic>=2.6.0
pyyaml>=6.0.1
python-frontmatter>=1.1.0
python-dotenv>=1.0.0

# HTTP & async
httpx>=0.27.0
aiosqlite>=0.19.0

# Numpy (used by genome engine for NN)
numpy>=1.24.0
